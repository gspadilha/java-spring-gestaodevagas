version: '3.8'

services:
  postgres:
    container_name: postgres_gestao_de_vagas
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=gestao_vagas
